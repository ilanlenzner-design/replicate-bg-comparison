# Replicate API Key (optional)
# If set, this will be used for all users instead of requiring them to enter their own key
# Also enables AI-powered image analysis using vision models
REPLICATE_API_KEY=r8_your_api_key_here

# Port (optional, Railway will set this automatically)
PORT=3000
